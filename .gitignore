# Python and virtual environments
venv/
env/
__pycache__/
*.pyc
.env

# Migration Tool Data / Secrets (DO NOT COMMIT)
liked.csv
headers.txt
oauth.json
progress.json
failed_songs.csv
logs.txt

* **Advanced Processing**: Dry-Run mode, track range processing (e.g. `100-500`), and 'Reverse Order' reading.
* **Logging System**: Automatically creates `logs.txt` for technical debugging and `failed_songs.csv` for manual review of missed tracks.
